## Mi primer restserver con Node

```
Se lleva a cabo la elaboración de la plantilla de inicio para el uso del rest server de NODE.JS

También se puede utilizar sin problema solo descargando

npm install
```